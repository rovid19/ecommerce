import React from "react";

const Homepage = () => {
  return (
    <main className="h-full w-full flex justify-center items-center">
      <section className="h-full w-[80%] items-center flex justify-center ">
        Homepage
      </section>
    </main>
  );
};

export default Homepage;
